{
  "name": "@ai-sdk-tools/root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run build:debug && bun run build:store && bun run build:artifacts && bun run build:devtools && bun run build:memory && bun run build:agents && bun run build:cache && bun run build:loveui-ai-tools",
    "build:store": "cd packages/store && bun run build",
    "build:artifacts": "cd packages/artifacts && bun run build",
    "build:devtools": "cd packages/devtools && bun run build",
    "build:memory": "cd packages/memory && bun run build",
    "build:agents": "cd packages/agents && bun run build",
    "build:cache": "cd packages/cache && bun run build",
    "build:loveui-ai-tools": "cd packages/loveui-ai-tools && bun run build",
    "build:debug": "cd packages/debug && bun run build",
    "build:example": "bun run build && cd apps/example && bun run build",
    "dev": "bun run -r --filter='@ai-sdk-tools/store' --filter='@ai-sdk-tools/devtools' --filter='@ai-sdk-tools/artifacts' --filter='@ai-sdk-tools/agents' dev",
    "dev:beta": "bun run -r --filter='@ai-sdk-tools/store' --filter='@ai-sdk-tools/devtools' --filter='@ai-sdk-tools/artifacts' --filter='@ai-sdk-tools/agents' dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "node scripts/check-beta-mode.js exit-if-beta && node scripts/clean-beta-versions.js && bun run build && node scripts/ensure-changesets.js && changeset version && changeset publish --tag latest",
    "changeset:beta": "changeset pre enter beta",
    "changeset:exit-beta": "changeset pre exit",
    "version-packages:beta": "changeset version",
    "release:beta": "bun run build:beta && changeset version && changeset publish --tag beta",
    "clean": "bun run -r --filter='@ai-sdk-tools/*' clean",
    "clean:beta": "bun run -r --filter='@ai-sdk-tools/store' --filter='@ai-sdk-tools/devtools' --filter='@ai-sdk-tools/artifacts' --filter='@ai-sdk-tools/agents' clean",
    "type-check": "bun run -r --filter='@ai-sdk-tools/*' type-check",
    "type-check:beta": "bun run -r --filter='@ai-sdk-tools/store' --filter='@ai-sdk-tools/devtools' --filter='@ai-sdk-tools/artifacts' --filter='@ai-sdk-tools/agents' type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.3",
    "@changesets/cli": "^2.29.7",
    "@types/node": "^24.10.0"
  },
  "dependencies": {
    "@base-ui-components/react": "1.0.0-beta.4",
    "@radix-ui/react-switch": "^1.2.6",
    "@supabase/supabase-js": "^2.81.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.553.0",
    "ogl": "^1.0.11",
    "tailwind-merge": "^3.4.0"
  }
}
